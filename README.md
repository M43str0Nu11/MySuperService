# MySuperService - Сервис прогноза погоды

## 📋 Описание
Веб-сервис для отображения усреднённого прогноза погоды для Новосибирска на основе данных от двух независимых источников.

## 🌤️ Источники данных
- **Open-Meteo** - европейский метеорологический API
- **7Timer!** - международный прогноз погоды (КНР)

Прогноз на: **7:00** (утро), **13:00** (день), **19:00** (вечер)

## 🏗️ Архитектура

### Docker контейнеры:
1. **web** (nginx) - веб-сервер для статических файлов
2. **db** (PostgreSQL) - база данных для хранения погодных данных
3. **parser** (Python) - парсер погоды, обновляется каждый день в 7:00
4. **api** (Flask) - REST API для получения данных

## 🚀 Быстрый старт

### Требования:
- Docker Desktop
- Git

### Установка:
```bash
git clone https://github.com/ваш-username/MySuperService.git
cd MySuperService
docker-compose up -d --build
```

### Доступ:
- **Сайт:** http://localhost:8080/pages/index.html
- **API:** http://localhost:5000/api/weather

## 📁 Структура проекта
```
MySuperService/
├── pages/              # HTML страницы
├── assets/
│   ├── css/           # Стили
│   └── js/            # JavaScript
├── api/               # Flask API
├── parser/            # Парсер погоды
├── logs/              # Логи ошибок
├── docker-compose.yml # Конфигурация Docker
├── init.sql           # Инициализация БД
└── README.md
```

## 🎯 Функционал
- ✅ Усреднённый прогноз от 2 источников
- ✅ Таблица с данными от каждого источника
- ✅ Кнопка "Обновить" для ручного обновления
- ✅ Автообновление каждые 5 минут
- ✅ Логирование ошибок

## 🛠️ Технологии
- **Frontend:** HTML, CSS, JavaScript
- **Backend:** Python (Flask), PostgreSQL
- **Парсинг:** requests, BeautifulSoup4
- **Инфраструктура:** Docker, Docker Compose, nginx

## 📊 База данных

### Таблица weather_data:
```sql
id, source, period, temperature, created_at
```

### VIEW weather_avg:
Автоматически усредняет последние данные от каждого источника.

## 🔧 Команды Docker

**Запуск:**
```bash
docker-compose up -d
```

**Остановка:**
```bash
docker-compose down
```

**Логи:**
```bash
docker logs mysuperservice-parser
docker logs mysuperservice-api
```

**Пересборка:**
```bash
docker-compose down
docker-compose up -d --build
```

## 🔒 Безопасность

⚠️ **Важно:** Пароли БД в `docker-compose.yml` - тестовые для локальной разработки.

Для продакшена:
1. Создай `.env` файл:
```env
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=weather_db
```

2. Измени `docker-compose.yml`:
```yaml
environment:
  POSTGRES_DB: ${POSTGRES_DB}
  POSTGRES_USER: ${POSTGRES_USER}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
```

3. Добавь `.env` в `.gitignore`

## 📝 Лицензия
MIT

## 👤 Автор
M435TR0 (https://github.com/M43str0Nu11)
